<!-- <p id="notice"><%= notice %></p> -->

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<% if @post.image.attached?%>
	<%= image_tag @post.image, size: "120"%>
<% end %>


<h4>Comments</h4>
<%= render @post.comments %>


<h4>Add a comment:</h4>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
